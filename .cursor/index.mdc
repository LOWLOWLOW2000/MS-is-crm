---
description: IS_01 — rule index (precedence, token budget)
alwaysApply: true
---

# Rules

1. `architecture.mdc` — stack, monorepo paths, docs
2. `core.mdc` — TS, React, Tailwind, tests, secrets
3. `git.mdc` — commits, branches, ignore/cache

Globs under `.cursor/rules/` override when scoped. Be concise; no duplicated prose across files.
